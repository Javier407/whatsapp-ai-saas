name: Deploy to production

on:
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to deploy (all / gateway / tenant-api / flow-engine / rag-indexer)'
        required: true
        default: 'all'

jobs:
  deploy:
    name: Deploy ${{ github.event.inputs.service }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        # Recommended: replace this step with appleboy/ssh-action
        # Example:
        #   uses: appleboy/ssh-action@v1
        #   with:
        #     host: ${{ secrets.VPS_HOST }}
        #     username: ${{ secrets.VPS_USER }}
        #     key: ${{ secrets.VPS_SSH_KEY }}
        #     script: |
        #       cd /opt/whatsapp-ai-saas
        #       git pull origin main
        #       if [ "${{ github.event.inputs.service }}" = "all" ]; then
        #         docker compose -f infra/docker-compose.yml build
        #         docker compose -f infra/docker-compose.yml up -d
        #       else
        #         docker compose -f infra/docker-compose.yml build ${{ github.event.inputs.service }}
        #         docker compose -f infra/docker-compose.yml up -d --no-deps ${{ github.event.inputs.service }}
        #       fi
        run: echo "[TODO: configure SSH deploy to your VPS — see step comment above]"
